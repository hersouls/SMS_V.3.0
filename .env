# =============================================================================
# Moonwave SMS V.3.0 - 환경 변수 설정
# =============================================================================
# 이 파일을 .env로 복사하고 실제 값으로 변경하세요.
# cp env.example .env

# =============================================================================
# Firebase Configuration (필수)
# =============================================================================
# Google OAuth Configuration (필수)
# =============================================================================
# Google OAuth 클라이언트 설정
VITE_GOOGLE_CLIENT_ID=your_google_oauth_client_id
VITE_GOOGLE_CLIENT_SECRET=your_google_oauth_client_secret

# =============================================================================
# GitHub Configuration (MCP용)
# =============================================================================
# GitHub Personal Access Token (MCP GitHub 서버용)
# GitHub Settings > Developer settings > Personal access tokens에서 생성
GITHUB_TOKEN=your_github_personal_access_token

# =============================================================================
# Firebase Configuration (필수)
# =============================================================================
# Firebase 프로젝트 설정 (필수)
VITE_FIREBASE_API_KEY=your_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
VITE_FIREBASE_MEASUREMENT_ID=your_measurement_id

# =============================================================================
# Application Configuration
# =============================================================================
# 애플리케이션 기본 설정
VITE_APP_URL=https://sub.moonwave.kr
VITE_APP_NAME=SMS V.3.0
VITE_APP_VERSION=3.0.0

# =============================================================================
# Development Configuration
# =============================================================================
# 개발 모드 설정
VITE_DEV_MODE=false
VITE_ENABLE_DEBUG=false

# =============================================================================
# Security Configuration
# =============================================================================
# CORS 허용 도메인 (쉼표로 구분)
VITE_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,https://sub.moonwave.kr,https://www.sub.moonwave.kr

# =============================================================================
# 설정 가이드
# =============================================================================
# 1. Firebase 설정:
#    - VITE_FIREBASE_API_KEY: Firebase 프로젝트 설정 > 일반 > 웹 앱에서 확인
#    - VITE_FIREBASE_AUTH_DOMAIN: Firebase 프로젝트 설정 > 일반 > 웹 앱에서 확인
#    - VITE_FIREBASE_PROJECT_ID: Firebase 프로젝트 설정 > 일반 > 웹 앱에서 확인
#
# 2. Google OAuth 설정:
#    - VITE_GOOGLE_CLIENT_ID: Google Cloud Console에서 생성
#    - VITE_GOOGLE_CLIENT_SECRET: Google Cloud Console에서 확인
#
# 3. GitHub 설정 (MCP용):
#    - GITHUB_TOKEN: GitHub Settings > Developer settings > Personal access tokens에서 생성
#    - 필요한 권한: repo, read:user, read:org
#
# 4. Firebase 설정 (필수):
#    - Firebase 프로젝트 설정 > 일반 > 웹 앱에서 확인
#
# 5. 보안 주의사항:
#    - .env 파일을 Git에 커밋하지 마세요
#    - 프로덕션에서는 VITE_DEV_MODE=false로 설정
#    - VITE_ENABLE_DEBUG=false로 설정 